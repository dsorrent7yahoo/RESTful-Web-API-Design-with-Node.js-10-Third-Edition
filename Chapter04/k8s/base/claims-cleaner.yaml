apiVersion: apps/v1
kind: Deployment
metadata:
  name: claims-cleaner
  namespace: healthcare
  labels:
    app: claims-cleaner
spec:
  replicas: 1
  selector:
    matchLabels:
      app: claims-cleaner
  template:
    metadata:
      labels:
        app: claims-cleaner
    spec:
      containers:
        - name: claims-cleaner
          image: localhost:5000/claims-cleaner:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4012
          envFrom:
            - configMapRef:
                name: microservices-config
            - secretRef:
                name: microservices-secret
          volumeMounts:
            - name: aws-credentials
              mountPath: /root/.aws
              readOnly: true
          readinessProbe:
            httpGet:
              path: /health
              port: 4012
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 4012
            initialDelaySeconds: 15
            periodSeconds: 20
      volumes:
        - name: aws-credentials
          secret:
            secretName: aws-credentials
---
apiVersion: v1
kind: Service
metadata:
  name: claims-cleaner
  namespace: healthcare
spec:
  selector:
    app: claims-cleaner
  type: NodePort
  ports:
    - protocol: TCP
      port: 4012
      targetPort: 4012
      nodePort: 30002
